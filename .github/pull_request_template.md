<!-- What this changes, and why. A linked issue counts as the why. -->

- [ ] `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and `cargo test --all-targets` pass
- [ ] Tried in a real terminal, if this touches the picker or a shell snippet
- [ ] Added a line under `Unreleased` in CHANGELOG.md, if users will notice
